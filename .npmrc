registry=https://registry.npmjs.org/
package-lock=false
# 使用官方 npm 源，避免镜像源同步滞后或 CDN 404 导致安装失败
# 如需国内镜像，可在本地开发环境临时设置：
#   npm config set registry https://registry.npmmirror.com --location=user
# 部署环境请保持使用官方源以保证稳定性